import React from "react";
import Chart from '../views/chart'

const Hero = () => (
  <div className="text-center hero">
    <h1 className="mb-4">IPC indicator</h1>
    <Chart />
  </div>
);

export default Hero;
